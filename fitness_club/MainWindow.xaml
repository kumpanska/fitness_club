<Window x:Class="fitness_club.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:fitness_club"
        mc:Ignorable="d"
        Title="FitnessClub" Height="600" Width="1100" MinHeight="400" MinWidth="600">

    <Window.Resources>
        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="Fitness Club"
                   FontSize="25"
                   FontWeight="Black"
                   Margin="10"
                   Foreground="Black"
                   VerticalAlignment="Top"
                   HorizontalAlignment="Left"
                   Grid.Column="0"/>
        <Grid Grid.Column="1" Background="SeaShell" Margin="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
          
            <Button Grid.Column="0" Background="CadetBlue" Style="{StaticResource RoundedButtonStyle}" Content="Фітнес-послуги" Margin="10,10,10,476" Foreground="Black" FontSize="20" FontWeight="DemiBold"/>
            <Button Grid.Column="1" Background="Black" Style="{StaticResource RoundedButtonStyle}" Content="Клієнти" Margin="10,10,10,476" Foreground="White" FontSize="20" FontWeight="DemiBold"/>
            <Button Grid.Column="2" Background="Goldenrod" Style="{StaticResource RoundedButtonStyle}" Content="Тренери" Margin="10,10,10,476" Foreground="Black" FontSize="20" FontWeight="DemiBold"/>
            <Button Grid.Column="3" Background="Brown" Style="{StaticResource RoundedButtonStyle}" Content="Розклад занять" Margin="10,10,10,476" Foreground="White" FontSize="20" FontWeight="DemiBold"/>
            <Button Grid.Column="4" Background="Plum" Style="{StaticResource RoundedButtonStyle}" Content="Рейтинг тренерів" Margin="10,10,10,476" Foreground="Black" FontSize="19" FontWeight="DemiBold"/>
        </Grid>
    </Grid>
</Window>
